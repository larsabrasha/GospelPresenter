<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <dict>
        <!-- The Mac App Store requires you specify if the app uses encryption. -->
        <!-- Please consult https://developer.apple.com/documentation/bundleresources/information_property_list/itsappusesnonexemptencryption -->
        <!-- <key>ITSAppUsesNonExemptEncryption</key> -->
        <!-- Please indicate <true/> or <false/> here. -->

        <!-- Specify the category for your app here. -->
        <!-- Please consult https://developer.apple.com/documentation/bundleresources/information_property_list/lsapplicationcategorytype -->
        <!-- <key>LSApplicationCategoryType</key> -->
        <!-- <string>public.app-category.YOUR-CATEGORY-HERE</string> -->
        <!-- The sign-in flow hands the device token back on this custom scheme. -->
        <key>CFBundleURLTypes</key>
        <array>
            <dict>
                <key>CFBundleURLName</key>
                <string>GospelPresenter Sign-In</string>
                <key>CFBundleURLSchemes</key>
                <array>
                    <string>gospelpresenter</string>
                </array>
            </dict>
        </array>
        <!-- NOTE: the live/projector view opens as a second MAUI Window, which Catalyst refuses
             ("The application does not support multiple scenes") without a UIApplicationSceneManifest.
             Do not add one. Declaring the manifest leaves the app with an empty window: MauiProgram
             runs, but App.CreateWindow is never called, so nothing is ever shown. Measured with the
             flag true and false, with and without a UISceneConfigurations entry naming
             MauiUISceneDelegate, with an app-owned subclass of it registered under its own ObjC
             name, and with UIDeviceFamily 2 and 2+6. A stock `dotnet new maui` project on this
             toolchain loses its window the same way, so this is MAUI or the Catalyst SDK, not us
             (MAUI 10.0.11, MacCatalyst SDK 26.2.10217, macOS 15.7). The projector needs a route
             that does not go through UIScene; see MauiLiveWindowLauncher. -->
        <key>UIDeviceFamily</key>
        <array>
            <integer>2</integer>
        </array>
        <key>UIRequiredDeviceCapabilities</key>
        <array>
            <string>arm64</string>
        </array>
        <key>UISupportedInterfaceOrientations</key>
        <array>
            <string>UIInterfaceOrientationPortrait</string>
            <string>UIInterfaceOrientationLandscapeLeft</string>
            <string>UIInterfaceOrientationLandscapeRight</string>
        </array>
        <key>UISupportedInterfaceOrientations~ipad</key>
        <array>
            <string>UIInterfaceOrientationPortrait</string>
            <string>UIInterfaceOrientationPortraitUpsideDown</string>
            <string>UIInterfaceOrientationLandscapeLeft</string>
            <string>UIInterfaceOrientationLandscapeRight</string>
        </array>
        <key>XSAppIconAssets</key>
        <string>Assets.xcassets/appicon.appiconset</string>
        <key>CFBundleName</key>
        <string>GospelPresenter</string>
        <key>UIBackgroundModes</key>
        <array>
            <string>fetch</string>
        </array>
        <key>ITSAppUsesNonExemptEncryption</key>
        <false/>
    </dict>
</plist>
